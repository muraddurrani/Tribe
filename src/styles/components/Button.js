import colours from '../colours'

const Button = {
  containerStyle: {
    width: 120,
    height: 40,
    borderRadius: 0
  },
  buttonStyle: {
    height: 40,
    borderRadius: 0
  }
}

export default Button